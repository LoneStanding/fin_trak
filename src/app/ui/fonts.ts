import { Inter } from 'next/font/google';
export const inter = Inter({ subsets: ['latin'] });

import {Fredoka} from 'next/font/google';
export const fred = Fredoka({
    weight: '400',
    subsets: ['latin'],
    display: 'swap'
});